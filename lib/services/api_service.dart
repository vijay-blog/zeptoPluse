class ApiService {
  // Production backend placeholder.
  // Replace baseUrl and implement authenticated HTTP calls when the Spring Boot
  // backend is ready.
  static const String baseUrl = 'https://api.yourdomain.com/api/v1';

  Future<void> createOrder(Map<String, dynamic> payload) async {
    throw UnimplementedError('Connect this method to the production backend.');
  }
}
